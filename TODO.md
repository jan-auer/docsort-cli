# Todo

- [ ] Delete shortcut in Browse mode — removes the highlighted file from the inbox. Requires explicit confirmation: hint bar prompts `delete? [y/N]`, only `y` or `yes` (case-insensitive) proceeds; any other key cancels.
